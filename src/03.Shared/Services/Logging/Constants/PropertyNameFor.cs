namespace Zeta.CodebaseExpress.Shared.Services.Logging.Constants;

public static class PropertyNameFor
{
    public const string Version = nameof(Version);
    public const string ApplicationName = nameof(ApplicationName);
    public const string AssemblyName = nameof(AssemblyName);
    public const string EnvironmentName = nameof(EnvironmentName);
}
